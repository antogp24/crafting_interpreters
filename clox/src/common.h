#ifndef _common_h
#define _common_h

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>

#define DEBUG_TRACE_EXECUTION
#define DEBUG_PRINT_PARSED_CODE
#define DEBUG_COMPILER_PARSING

#define ARRAY_SIZE(A) (sizeof(A) / sizeof(*A))

#endif // _common_h
